def includes = ['lib-common', 'lib-logging', 'lib-commonbase', 'lib-json', 'api-data-store-server-java',
'lib-two-phase-transaction', 'api-openbis-java', 'api-data-store-server-javascript',
'api-openbis-javascript', 'lib-openbis-common', 'lib-db', 'lib-dbmigration', 'lib-authentication',
'lib-pathinfo-db', 'server-original-data-store', 'server-application-server'
]

includes.forEach { name ->
    includeFlat(name)
    project(":${name}").projectDir = new File("../../../../../../../../../../${name}")
}