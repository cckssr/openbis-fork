#
# Any changes on these properties we discourage outside the development team
# These properties allow to swap internal components during development
# They are used by the development team for testing
#
# prefix needed for setting service.properties from system properties or environment variables
system.property.prefix=afssftp.
logFactoryClass=ch.ethz.sis.shared.log.standard.impl.StandardLogFactory
afssftp.logging.configuration=./etc/logging.properties

serverPort=2223
applicationServerUrl=http://localhost:8888
afsUrl=http://localhost:8085/afs-server

keyStorePath=./etc/openBIS.keystore
keyStorePassword=changeit
keyStoreKeyAlias=cisd

maxFileChannelsPerSession=20
maxAfsClientChunkSize=1048576
afsCacheTimeoutMillis=300

# EntityTypePermId value of the type of EXPERIMENT entities created through SFTP:
# - must support the property NAME
# - defaults to COLLECTION
createdExperimentType=

# EntityTypePermId value of the type of SAMPLE entities created through SFTP:
# - must support the property NAME
# - defaults to ENTRY and cannot be FOLDER
createdSampleType=
