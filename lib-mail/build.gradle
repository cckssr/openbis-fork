apply from: '../build/javalibrary.gradle'

dependencies {
    api project(':lib-commonbase'), libs.javax.activation.activation, libs.com.sun.mail.javax.mail
    testImplementation libs.org.testng.testng
}